@{
  Layout = "_Layout";
}

@model AnniesAnesthesia.Models.Doctor;

<h2>Doctor Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</h3>
<br>

@if(@Model.JoinEntitiesSpecialty.Count == 0)
{
  <p>This doctor does not have any Specialty</p>
}
else
{
  <h4>The Doctor specialties are:</h4>
  <ul>
  @foreach(var join in Model.JoinEntitiesSpecialty)
  {
    <li>@join.Specialty.SpecialtyName</li>
  }
  </ul>
}

<p>@Html.ActionLink("Back to doctors", "Index")</p>
<p>@Html.ActionLink("Edit doctor", "Edit", new { id = Model.DoctorId })</p>
<p>@Html.ActionLink("Delete doctor", "Delete", new { id = Model.DoctorId })</p>