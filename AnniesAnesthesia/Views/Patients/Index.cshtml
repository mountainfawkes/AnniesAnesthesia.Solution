@{
  Layout = "_Layout";
}

@using AnniesAnesthesia.Models;
@model List<AnniesAnesthesia.Models.Patient>;

<h1>Patient</h1>

@if (@Model.Count == 0)
{
  <h3>No patients have been added yet!</h3>
} 

@foreach (Patient patient in Model)
{
  <li>@Html.ActionLink($"{patient.Name}", "Details", new { id = patient.PatientId })</li>
}

<p>@Html.ActionLink("Add new patient", "Create")</p>